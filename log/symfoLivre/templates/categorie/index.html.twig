{% extends 'base.html.twig' %}

{% block title %}Hello CategorieController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div
	class="container">
	<!-- User main content -->
	<user class="col-md-12 maincontent">
		<div class="container">
			<div class="row">
				<h3>Les categories</h3>
				<hr style="width:100%; height:10px; background-color:#1B4F72 ">
				<table class="table">
					<thead>
						<tr>
							<th>Id</th>
							<th>nom</th>
                            
							<th>Action</th>
						</tr>
					</thead>

					<tbody>
						{% for categorie in categorie %}
							<tr>
								<td>
									{{categorie.id}}
								</td>
								<td>
									{{categorie.nomCategorie}}
								</td>
								<td>
<th><a href="{{path('categorie_display', {'id': categorie.id} ) }}" class="btn btn-dark"> Affichage</a> </th>
                                    <th><a href="{{path('categorie_edit', {'id': categorie.id} ) }}" class="btn btn-dark"> Edit</a> </th>
                                    <th>{{ include('categorie/_delete_form.html.twig') }}   </th>
                                                                        

								</td>

							</tr>

						{% endfor %}

					</tbody>
				</table>

			</div>
		</div>
	</cat>
</div>

{% endblock %}
