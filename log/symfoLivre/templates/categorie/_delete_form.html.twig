<form method="post" action="{{ path('cate_sup', {'id': categorie.id}) }}" onsubmit="return confirm('Etes vous sûre de vouloir supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie.id) }}">
    <button class="btn btn-danger">Suppr</button>
</form>
