{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}

   <div class="container">
	<!-- User main content -->
	<user class="col-md-12 maincontent">
		<div class="container">
			<div class="row">
				<h3>Utilisateur / {{ user.nomUser }}</h3>
				<hr style="width:100%; height:10px; background-color:#1B4F72 ">


                                  <table class="table">
                        <tbody>
                            <tr>
                                <th>Id</th>
                                <td>{{ user.id }}</td>
                            </tr>
                            <tr>
                                <th>NomUser</th>
                                <td>{{ user.nomUser }}</td>
                            </tr>
                            <tr>
                                <th>MailUser</th>
                                <td>{{ user.mailUser }}</td>
                            </tr>
                            <tr>
                                <th>password</th>
                                <td>{{ user.password }}</td>
                            </tr>
                            <tr>
                                <th>RoleUser</th>
                                <td>{{ user.roleUser }}</td>
                            </tr>
                        </tbody>
                    </table>

                    <a href="{{ path('app_user_index') }}" class="btn btn-primary">Liste des Utilisateurs</a> <br><br>

                    <a href="{{ path('app_user_edit', {'id': user.id}) }}" class="btn btn-warning">editer.... </a>

                    {{ include('user/_delete_form.html.twig') }}
            </div>
        </div>
    </user>
</div>

{% endblock %}
