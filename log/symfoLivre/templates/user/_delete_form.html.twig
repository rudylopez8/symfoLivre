<form method="post" action="{{ path('adm_users_suppr', {'id': user.id}) }}" onsubmit="return confirm('Etes vous sûre de voulir supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn btn-danger">Suppr</button>
</form>
