{% extends 'base.html.twig' %}

{% block body %}

        {% include("menus/navigation_admin.html.twig") %}
        {% include("headers/header_admin.html.twig") %}

    <div class ="col-lg-12 bg-light">
        <div class ="container-sm">   <div>
                <h3> Gestion des utilisateurs </h3>
                <hr>
        </div>

         <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th><a href="{{path('app_users_tri_asc', {'champ': 'noms'} ) }}" >Noms</a></th>
                <th><a href="{{path('app_users_tri_asc', {'champ': 'prenoms'} ) }}" >Prenoms</a></th>
                <th><a href="{{path('app_users_tri_asc', {'champ': 'adresse'} ) }}" >Adresse</a></th>
                <th><a href="{{path('app_users_tri_asc', {'champ': 'mail'} ) }}" >Mails</a></th> 
                <th><a href="{{path('app_users_tri_asc', {'champ': 'phone'} ) }}">Tel</a></th>
                <th>Action</th>
            </tr>
        </thead>

        <tbody>
            {% for user in users %}
                <tr>
                    <td>{{ user.id }}</td>
                    <td>{{ user.noms }}</td>
                    <td>{{ user.prenoms }}</td>
                    <td>{{ user.adresse }}</td>
                    <td>{{ user.mail}}</td>
                    <td>{{ user.phone}}</td>
                    <td> 
                            <div class="card-footer p-4 pt-0 border-top-0 bg-transparent">
                                <div class="text-center">
                                    <th><a href="{{path('adm_users_display', {'id': user.id} ) }}" class="btn btn-dark"> Display</a> </th>
                                    <th><a href="{{path('adm_users_edit', {'id': user.id} ) }}" class="btn btn-dark"> Edit</a> </th>
                                    <th>{{ include('users/_delete_form.html.twig') }}   </th>
                                    {#<a href="{{path('adm_users_suppr', {'id': user.id} ) }}" class="btn btn-dark"> Suppr</a> #}
                                </div>
                            </div> 
                    
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="5">Aucun article pour lemoment !</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
<a href="{{path('adm_users_new')}}" class="btn btn-dark"> Ajouter 1 Utilisateur /  </a> <br><br><br><br
    </div></div></div>


{% endblock %}




